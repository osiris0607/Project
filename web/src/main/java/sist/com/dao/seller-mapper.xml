<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="sist.com.vo.SellerinfoVO">

	<select id="sellerIdCheck" parameterType="java.lang.String"
		resultType="java.lang.String">
		SELECT PASSWORD FROM SELLERINFO
		WHERE ID=#{VALUE}
	</select>

	<select id="selectSeller" resultType="sellermember">
		SELECT *
		FROM SELLERINFO
	</select>

	<select id="selectSellerInfo" resultType="sellermember"
		parameterType="java.lang.String">
		SELECT *
		FROM SELLERINFO
		WHERE ID=#{value}
	</select>



	<select id="sellerNumCheck" parameterType="java.lang.String"
		resultType="java.lang.String">
		SELECT NUM FROM SELLERINFO
		WHERE NUM=#{value}
	</select>

	<select id="selectAddress" parameterType="java.lang.String"
		resultType="sist.com.vo.ZipcodeBean">
		SELECT ZIPCODE,SIDO,GUGUN,DONG,NVL(BUNJI,' ')BUNJI
		FROM
		ZIPCODE
		WHERE DONG LIKE '%'||#{VALUE}||'%'
	</select>

	<insert id="insertSellerInfo" parameterType="sellermember">
		INSERT INTO
		SELLERINFO( NO, ID, PASSWORD, NAME, NUM, REP, EMAIL, TEL, CP)
		VALUES(
		SELLERINFO_SEQ.NEXTVAL,#{id},#{password},#{name},#{num},#{rep},#{email},#{tel},#{cp})
	</insert>

	<select id="selectTitleName" resultType="java.util.List">
		SELECT PRGROUP_NAME FROM PRODUCT_GROUP WHERE PRGROUP_LEVEL=1
	</select>


	<select id="viewProduct" resultType="sist.com.ProductVO"></select>







</mapper>
